# 更新日志

## V2.2.0 (2026-08-29)

### UI 优化
- GUI 启动时默认最大化
- 主内容区支持随窗口尺寸伸缩，底部操作按钮区固定显示
- 日志区收起后贴靠窗口底部，展开时可拖动顶部边缘调整高度
- 统一按钮、输入框、树表、滚动条和页签的视觉样式
- 优化侧栏宽度、选中态与悬停态
- 导航切换和任务页签切换时同步更新状态提示
- 优化操作按钮悬停、禁用和日志操作反馈

## V2.1.0 (2026-08-27)

### 重构: 左侧双大导航 (方案 B: 自绘侧边栏)
- 新增 `gui/common/sidenav.py`: 大卡片式侧边导航 (主标题+副标题, 选中主色高亮/悬停反馈)
- 大Tab "A2L处理" → 横向子 Tab: 地址映射/合并A2L/更新A2L (**子 Tab 布局与旧版完全一致**)
- 大Tab "在线标定" → 原 CalibTab 整页承载 (INCA 风格)
- 日志坞/状态栏/进度条保持全宽共享; 全局按钮跟随当前子 Tab, 在线标定页自动隐藏
- 快捷键: Ctrl+1~3 → A2L处理+对应子Tab; Ctrl+4 → 在线标定; F5 运行
- 导航位置跨会话记忆 (last_nav/last_sub, appstate)
- BaseTab 支持非 Notebook 容器挂载 (isinstance 判定, 契约不变)
- 在线标定: 左树标题改用树自带 heading 行, 与右侧表格 header 顶部严格对齐 (原独立 Label 错位)
- 页面切换保留 pack 方案; 【回退】日志区分割面板/启动最大化等改动在部分环境触发 ttk 渲染缺陷 (mapped 但空白), 已整体撤销并恢复原日志区布局

## V2.0.0 (2026-08-27)

### 重构: UI 架构组件化 (行为等价的大版本)
- **gui/app.py 从 794 行瘦身到 ~200 行纯壳**: 横幅/页签容器/全局按钮/状态栏/运行态服务
- 新增分层: `gui/theme.py` (设计令牌: 色板/8px间距/字号阶梯/语义状态色 + ttk 样式工厂)
- 新增 `gui/common/`: LogDock 日志坞 (线程安全队列渲染/着色/导出/**可折叠**)、FileRow/PathListbox 复用件
- 新增 `gui/tabs/`: BaseTab 契约 (TITLE/DESC/HAS_RUN/RUN_LABEL/HAS_TEST) + 注册表驱动
  - addr.py 地址映射 / merge.py 合并A2L / sync.py 更新A2L (全部从 app.py 搬迁)
  - calib/ 在线标定迁入并对齐 BaseTab (自包含页签 HAS_RUN=False)
- **交互增强**: F5 运行当前页任务; Ctrl+1~4 切页签; 全部页签最近路径记忆 (appstate); 同步/地址更新分阶段状态栏文案
- 全功能回归通过: 四页签契约/快捷键/真实解析/在线标定三修(单步撤销+热图实时+自适应)

## V1.9.3 (2026-08-27)

### 新功能: 「更新A2L」内嵌自动地址更新 (流程串联)
- 新增勾选"同步完成后自动更新地址 (ELF/MAP → 输出 A2L)": 勾选后展开地址文件选择行, 按扩展名自动识别 ELF/MAP
- 同步完成后对输出 A2L **整体**执行地址刷新, 统计(变量/匹配/更新/未匹配)并入完成弹窗与日志
- 符合真实工作流: 先合成整车 A2L, 再整体刷新地址, 一步直达; 独立的「地址映射」页签保留
- 未选地址文件时启动被拦截并提示

### 决策记录
- 前三个页签(地址映射/合并A2L/更新A2L)经评估**不合并**: 三者是不同操作共用外壳, 非功能重复; 历史上曾因单页 RadioButton 不直观改为页签, 不回退

## V1.9.2 (2026-08-27)

### 重构: 移除「DCM写入HEX」页签 (功能合并)
- 原 Tab4 与「在线标定」功能重复: 在线标定的 导入DCM → 保存HEX 已完整覆盖其能力, 且多出限值校验/撤销/HEX对比
- 移除独立页签及相关浏览/处理代码; 引擎函数 `dcm_to_hex` 保留于 `core.dcm_handler` (未来 CLI 批量处理可复用)
- 页签由 5 个变为 4 个: 地址映射 / 合并A2L / 更新A2L / 在线标定

## V1.9.1 (2026-08-27)

### 修复
- **Ctrl+Z 无效果/连撤两步**: 焦点在编辑器内时, 对话框与主窗口两层快捷键沿 bindtags 连续触发导致一次按键撤销两步; 对话框快捷键返回 break 截断传播; 主窗口触发撤销时同步刷新所有打开的编辑器视图; 空栈时日志提示"无可撤销"
- **MAP 热图不实时渲染 (致命)**: MapEditor 的 FigureCanvasTkAgg 实例只存在局部变量, 未挂到 self.canvas_mpl, 每次重绘 draw_idle 都抛 AttributeError 被 except 吞掉; colorbar 改为复用 update_normal 防重复创建; tight_layout 异常与绘制隔离, draw_idle 必达
- **编辑窗口尺寸**: 移除写死的 1250x900 上限, 新增基类 _fit_geometry —— 按网格行列数推算内容所需尺寸并与屏幕可用区 92% 取小居中; MAP/曲线/轴点编辑器统一适配
- 弹窗错误信息格式化兼容字符串与元组混合的错误列表

### 回归验证 (PedalMap DrvReq_grdTqCrpUp_M 14x13)
- 双焦点场景 Ctrl+Z 单步撤销 + 视图同步 + 重做闭环 + 空栈安全
- 热图 image 数组随单元格编辑实时变化, colorbar 实例复用
- 14 列 MAP 窗口 1346px = 内容所需宽度, 无横向滚动

## V1.9.0 (2026-08-26)

### 新功能: 数据集管理 (P2, 在线标定页签)
- **导出 DCM**: 把当前标定值导出为 KONSERVIERUNG_FORMAT 2.0 数据集; 可选"仅导出已修改对象"或"导出全部"; 支持 FESTWERT/FESTWERTEBLOCK/GRUPPENKENNLINIE/GRUPPENKENNFELD/STUETZSTELLENVERTEILUNG, MAP 按 Y 外层逐行 WERT 输出, 自动携带轴值 (ST/X-ST/Y)、单位、长标识与 FUNKTIONEN 段
- **导入 DCM**: 解析 DCM 并应用到当前会话 (含曲线/MAP 关联轴点的联动写入); 维度/限值校验, 超限写入被拒绝; **整次导入为单个撤销单元 (Ctrl+Z 整体还原)**
- **HEX 对比**: 当前内存与任意 HEX 逐字节对比, 报告差异字节总数、按对象统计改动格数 (精确到对象名), 输出对比报告到日志
- **最近文件持久化**: 记住上次使用的 A2L/HEX 路径与分组模式 (存于 ~/.a2l_toolbox_session.json), 下次启动自动填充

### 修复 (P1 反馈)
- 修复标定值编辑器缺少"确定/应用"按钮的问题
- 修复 CellGrid 事件被子窗口拦截导致 MAP/曲线无法点击编辑的问题
- 修复 VAL_BLK (_CA 数组) 编辑器空白的问题 (改为纵向向量 + 柱状图)
- 修复曲线/MAP 轴值显示为空的问题 (AXIS_PTS 解析顺序缺陷 + 轴地址为 0x0000 时降级为索引)
- MAP/曲线编辑器顶部显示 X/Y 轴对应的 AXIS_PTS 对象名 (如 _AX/_AY), 轴头可直接双击编辑轴点值
- 轴点 (AXIS_PTS) 对象纳入标定量列表, 可搜索/编辑/还原

## V1.8.0 (2026-08-26)

### 重构: 在线标定全面对标 INCA (P1)
- 架构拆分: 新增 `core/calib_engine.py` (CalibSession 会话引擎) 与 `gui/calib/` 包 (tab/editors/widgets), 主程序 app.py 由约 2000 行精简至 860 行
- 数据内核 (P0):
  - 解析 COMPU_METHOD 的 Units/Format、RECORD_LAYOUT 的 FNC_VALUES 真实数据类型 (不再按名称猜测)、LOWER/UPPER 限值
  - 支持 FIX_AXIS_PAR 隐式轴值生成; AXIS_PTS 独立对象; MEASUREMENT 元数据; GROUP 树层级
  - 加载时原始字节快照, 单元格级脏标记; 命令式 Undo/Redo (Ctrl+Z/Y); 单对象还原
  - MAP 按 COLUMN_DIR/ROW_DIR 存储方向正确映射网格坐标与内存地址
- 浏览与搜索:
  - FUNCTION / GROUP / 平铺三种分组模式切换, 树形浏览
  - 搜索框实时过滤 (250ms 防抖, 支持 `*` 通配符); "仅显示已修改"过滤
  - 表格新增单位列 (来自 COMPU_METHOD)、原始值对比列、修改状态列; 已修改行蓝色高亮
- 编辑器:
  - VALUE: Spinbox + 滑块联动, 限值范围约束
  - CURVE: matplotlib 折线预览 + 网格编辑, X 轴点可编辑并联动写回 AXIS_PTS 地址
  - MAP: matplotlib 热图预览 + INCA 风格色阶背景网格 (绿→黄→红渐变)
  - 轴点独立编辑器 (AXIS_PTS)
- 编辑辅助: 选区线性插值 (曲线按行/MAP 按行列), TSV 复制粘贴与 Excel 互通, 右键菜单 (编辑/还原/复制名称)
- 快捷键: Ctrl+S 保存 HEX (首次保存自动备份 .bak), Ctrl+F 定位搜索, Ctrl+Z/Y 撤销重做
- 写入保护: 超出 LOWER/UPPER 限值的写入被拦截并提示

### 其他
- 新增依赖: matplotlib>=3.5 (requirements.txt; 未安装时编辑器自动降级为无图形模式)
- PyInstaller spec 同步更新 hiddenimports

## V1.7.0 (2026-08-26)

### 新功能: 在线标定 (第 5 个页签)
- 加载 A2L 和 HEX 文件，显示所有标定量并支持在线修改值
- 按 FUNCTION 分组：左侧树形视图按 FUNCTION 组织标定量，类似 INCA
- 模糊搜索：支持 `*` 通配符匹配标定量名称
- INCA 风格编辑器：曲线显示为 X/Y 两列并排，MAP 显示为二维表格
- 双击直接编辑：双击表格单元格即可修改值
- 轴数据加载：自动加载 AXIS_PTS 的 X 轴/Y 轴值并显示
- 物理值转换：根据 COMPU_METHOD 的 COEFFS 正确转换物理值与原始值
- 整数类型处理：SWORD/UWORD 等整数类型自动四舍五入
- 写入验证：写入后从内存读回实际物理值显示

### 架构优化
- 模块化拆分：单文件 2490 行拆分为 10 个模块
  - `core/` - 核心功能模块 (elf_parser, map_parser, a2l_parser, sync_engine, dcm_handler, hex_handler)
  - `gui/` - GUI 模块 (app.py)
  - `utils/` - 工具模块 (config.py, logger.py)
- 配置集中管理：版本号、配色方案、文件类型定义集中在 utils/config.py
- 代码可维护性提升：平均每个模块 200-300 行

### UI 优化
- "更新地址"更名为"地址映射"，减少歧义
- 新增第 5 个页签"在线标定"
- 弹窗居中显示
- 标定量表格显示类型信息（如 CURVE[10]、MAP[3x4]）

### 修复
- 修复 Record Layout 名称（如 Lookup2D_WORD、Scalar_SWORD）无法正确识别数据类型的问题
- 修复 VAL_BLK 类型数组大小未从 A2L 的 NUMBER 字段解析的问题
- 修复地址为 0x0000 的标定量被错误跳过的问题
- 修复 MAP 编辑后标定量列表只显示当前 MAP 的问题
- 修复 MAP 值编辑时索引错位的问题

## V1.6.1 (2026-08-22)

### DCM写入HEX 修复 (回导 DCM 与原始 DCM 差异巨大的问题)
- 修复: MAP (GRUPPENKENNFELD) 数值写入顺序错误 — DCM 的 WERT 按 ST/Y 逐行存放 (轴2外层/轴1内层), 而 HEX 的 FNC_VALUES COLUMN_DIR 按轴1外层/轴2内层存储, 此前未转置直接顺序写入, 导致非对角 MAP 回导后数值全部错位; 现按存储方向转置后写入 (ROW_DIR 布局保持 DCM 顺序)
- 修复: MAP/曲线的轴映射改为按 AXIS_DESCR 声明顺序 (第1个 AXIS_DESCR ← ST/X, 第2个 ← ST/Y), 不再按 AXIS_PTS 地址升序猜测 (地址顺序与声明顺序不一致时轴会被写反/维度校验误判)
- 修复: 维度校验改为按声明顺序逐轴对比, 此前 31 个 MAP 被误判维度不一致而跳过; 现全部正确写入 (MAP 写入数 313 → 344)
- 新增: 支持 FIX_AXIS 隐式轴 (FIX_AXIS_PAR/FIX_AXIS_PAR_DIST, 轴不占 ECU 存储) — 只写数值区, 不再整对象跳过 (修复 VehV_rSteerForSlopEna_M, VLC_facPSpdCtlGain_T 等未写入)
- 改进: 曲线轴点数与 A2L 不一致时不再强行写入 (可能越界污染相邻内存), 改为整对象跳过并警告; 值个数与维度乘积不符时同样跳过; 统计汇总新增"维度不一致"计数; RECORD_LAYOUT 索引新增 FNC_VALUES 存储方向解析 (COLUMN_DIR/ROW_DIR)
- 验证: 全量往返测试 676 个曲线/MAP 对象 (含全部轴) 从输出 HEX 解码后与 DCM 逐值比对, 676/676 全部通过 (修复前 223 个 MAP 不一致)

## V1.6.0 (2026-08-21)

### 新功能: DCM写入HEX (第 4 个页签)
- 解析 DCM 标定文件 (KONSERVIERUNG_FORMAT, 支持 FESTWERT/FESTWERTEBLOCK/GRUPPENKENNLINIE/GRUPPENKENNFELD/FESTKENNLINIE 及多行续行), 结合 A2L 的 ECU 地址/记录布局/转换公式将物理值编码为原始字节, 按地址写入 Intel HEX 并输出新文件 (不修改原 HEX)
- 支持标定量/块、曲线、MAP (COM_AXIS 轴数据写入对应 AXIS_PTS 地址); RAT_FUNC 按系数正向求值编码 (Simulink 生成的 COEFFS 为 phys→raw 方向, longid 形如 "Q = V*100"), 支持 FLOAT32/FLOAT64 直接打包; 字节序默认小端, 遵循 COMPU_METHOD 显式声明
- MAP 轴按 AXIS_PTS 地址升序映射 ST/X→ST/Y, 规避 AXIS_DESCR 声明顺序不确定的问题; MAP 维度与 A2L 声明不一致时整对象跳过并警告
- HEX 逐条校验和验证, 输出保持 32 字节数据记录与扩展线性地址记录风格; 地址越界/A2L 无对象/转换不支持 (如 TAB_VERB) 分类统计跳过并记录警告
- 后台线程执行, 复用进度条/日志批量渲染/状态栏/日志自动保存体系
- 修正 (验证发现): 初版误按 ASAM raw→phys 方向对 COEFFS 反解编码, 导致标定量写入值普遍错误 (回导 DCM 差异巨大); 现按 Simulink 约定正向求值, 经全量验证 106832 个写入值均能正确还原为 DCM 值

## V1.5.1 (2026-08-21)

### 更新A2L
- 新增对象不再在文件末尾另起 "merged from model A2L" 标记区段, 改为直接原位插入 Base A2L 中该模型原有数据区域之后 (目标中无该模型已有对象时才插入 /end MODULE 之前)
- GROUP 块不再同步/保留: 合并时跳过源文件的全部 GROUP; 勾选清理时, Base A2L 中属于该模型的旧 GROUP 一并删除, 并自动清理内容已清空的旧版标记区段
- 修复: 清理时误将同名的模型 FUNCTION 名称行当作悬空引用删除, 导致 FUNCTION 损坏 (引用行清理仅限子节区)
- 修复: 新增块以多行整体元素插入行列表, 导致后续清理阶段行号与块范围错位 (错位量=新增块总行数), 模型自身 FUNCTION 内的引用被误判为块外引用, 旧对象全部无法删除并产生大量"引用保留警告"; 现新增块与替换块一致拆为单行元素
- 修复: GROUP 之间 SUB_GROUP 互相引用及 longid 路径串中的同名出现被误计为块外引用, 导致旧 GROUP 无法删除

### UI 优化
- 优化: 运行时进度条卡顿 — 日志改为工作线程入队、主线程每 100ms 批量渲染 (此前每条日志都调度一次 after 并强制滚动, 阻塞事件队列与进度条动画)
- 优化: 线程结束/弹窗前先刷出队列中未渲染的日志, 避免日志尾部丢失或弹窗先于日志显示

## V1.5.0 (2026-08-21)

### UI 优化
- 整体换肤: 深蓝标题横幅 + 卡片式页签 + 现代扁平配色, 使用 clam 主题统一样式
- 文件选择区改为分组框 (符号源 / A2L 文件 / 文件选择), 层次更清晰
- 主操作按钮改为主色调强调样式, 日志区/清空/保存按钮右对齐
- 运行时显示动画进度条, 状态栏按状态变色 (处理中蓝 / 成功绿 / 出错红)
- 日志按内容自动着色: 错误红色, 警告橙色, 完成/保存绿色, 分隔线弱化
- 开发者信息与 GitHub 链接移至顶部横幅右侧

### 代码质量
- 工作线程的日志/弹窗/状态栏更新全部改为 root.after 线程安全调度
- DPI 感知设置移至窗口创建之前, 高分屏下首次打开即清晰
- 日志区改为只读 (disabled), 避免误输入

### 修复
- 更新A2L支持同步新版模型的 FUNCTION 块: 同名 FUNCTION 以新版为准整体更新, 目标没有的 FUNCTION 新增 (此前 FUNCTION 块被直接跳过, 不做任何更新)
- 更新A2L清理旧版遗留对象时, 模型自身 FUNCTION 块 (如 AMBT_AmbTSen) 内的引用不再阻止删除, 消除大量"引用保留警告"; 删除后同步清理这些 FUNCTION 块中的引用行, 避免悬空引用 (源无 FUNCTION 时的兜底逻辑)
- 其他模型 FUNCTION 块中的引用仍会正确阻止删除并告警

## V1.4.1 (2026-08-20)

### 界面
- 新增程序图标 (app.ico)，打包后的 exe 与运行时窗口均显示新图标；图标采用简洁大色块设计并按尺寸独立绘制，小尺寸 (16/32px) 下依然清晰
- 标题下方显示开发者信息 (Henry)，GitHub 链接可点击跳转

### 代码质量
- 任务运行期间禁用"开始"按钮，防止重复点击启动多个线程并发写同一输出文件，结束后自动恢复
- 裸 except 改为 except Exception (日志保存/DPI/图标加载)
- 移除未使用的 json 导入及无占位符的冗余 f-string
- "更新A2L"完成弹窗在未勾选清理时不再显示"删除旧版遗留"行

## V1.4.0 (2026-08-20)

### 界面
- "更新地址"和"合并A2L"页签顶部增加功能说明文字，与"更新A2L"页签风格一致
- 修复状态栏在窗口未最大化时被日志区遮盖的问题：状态栏改为先打包并固定底部，日志区默认高度缩小至 10 行，并设置窗口最小尺寸 750x520

### 新增"更新A2L"功能 (第三个 Tab)
- 支持同时添加多个新版模型 A2L（列表多选），依次同步进同一个整车 A2L，各模型归属独立判定、互不干扰
- 将新版模型 A2L（如 Simulink Coder 生成）的变量、标定量、曲线、MAP 等同步进整车 A2L
- 智能替换：同名对象用新版内容更新，同时保留整车 A2L 中已有的真实 ECU 地址（0x0000 占位符不覆盖真实地址）
- 新增对象自动插入 /end MODULE 之前，带 `merged from model A2L` 标记区段
- GROUP 按长标识路径匹配，正确处理新旧版本子系统改名
- 多模型同名对象保护：目标存在多个同名块且内容不一致时保持不变，避免误改其他模型
- 可选"删除旧版遗留对象"：旧版有而新版没有的对象（按 COMPU_METHOD 前缀/引用归属判定）安全删除，删除前检查块外残留引用，有引用则保留并告警
- 可选目标文件自动备份 (.bak)，输出与目标同路径且未备份时弹确认框
- utf-8 + surrogateescape 无损读写大文件，保留原始换行符

## V1.3.0 (2026-08-12)

### 修复
- 修复头文件 Data 注释标记匹配失败的问题，优化正则为宽松模式兼容不同格式的 `*` 间距
- 修复合并提取变量为空的问题：get_a2l_blocks 会跳过 PROJECT/MODULE 等外层容器，直接定位目标块类型
- 修复 Data 标记行自身未被写入输出文件的问题
- 修复 COMPU_METHOD 块名称匹配失败导致丢失的问题：正则兼容 /* Name of CompuMethod */ 格式

### 合并功能重构
- 合并逻辑重构为"提取 + 组装"两步模式
- 提取内容去除外壳，只保留 RECORD_LAYOUT、变量定义、COMPU_METHOD、FUNCTION 等实际内容
- 新增头文件 A2L 选项：可将合并内容插入指定头文件的 `Data` 注释标记处
- 不指定头文件时，输出去壳的纯内容 A2L（不含 ASAP2_VERSION/PROJECT/MODULE 外壳）
- FUNCTION 块按源文件分组，自动生成 DEF/REF/IN_MEASUREMENT 等子项

## V1.2.3 (2026-08-10)

### 文件重命名
- 主程序由 `update_a2l_gui.py` 更名为 `a2l_toolbox.py`，更好反映工具箱的多功能定位
- 同步更新 `build.bat`、`A2L_Address_Updater.spec` 和 `README.md` 中的引用

## V1.2.2 (2026-08-10)

### UI 优化
- 界面模式选择从 RadioButton 改为 Tab 页签，操作更直观
- "更新地址"和"合并A2L"分为两个独立的标签页

## V1.2.1 (2026-08-10)

### 修复
- 修复代码中版本号 (v8) 与 CHANGELOG (V1.2.0) 不一致的问题，统一为 V1.2.0

## V1.2.0 (2026-08-10)

### 合并逻辑优化
- 对齐 INCA 导出的参考 A2L 格式
- FUNCTION 分组逻辑完善，顶层增加 VCU_MODULE SUB_FUNCTION 汇总

## V1.1.0 (2026-08-09)

### 新增
- 新增"合并文件"模式，支持将多个 A2L 文件合并为一个
- 新增 MAP 文件解析支持
- 新增"删除未匹配变量"选项

## V1.0.0 (2026-08-08)

### 初始版本
- ELF 文件解析（32/64 位）
- A2L 变量地址精确匹配更新
- 图形化界面
